/**
 * Application-wide constants
 */
export const STORAGE_KEYS = {
  TOKEN: process.env.TOKEN_KEY || 'auth_token',
};

/**
 * Navigation route names used throughout the application
 * @constant {Object}
 */
export const ROUTES = {
  SPLASH: 'Splash',
  LANDING: 'Landing',
};

/**
 * Timing constants used in the application
 * @constant {Object}
 */
export const TIMING = {
  SPLASH_SCREEN_DELAY: 2000,
};
