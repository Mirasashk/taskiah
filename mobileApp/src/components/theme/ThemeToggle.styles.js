/**
 * Styles for the ThemeToggle component
 * @param {Object} theme - The current theme object
 * @returns {Object} Style object for the component
 */
export const styles = {
  container: theme => ({
    padding: theme.spacing.sm,
    marginRight: theme.spacing.sm,
  }),
};
