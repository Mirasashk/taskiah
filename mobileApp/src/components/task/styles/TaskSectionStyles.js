import {StyleSheet} from 'react-native';

export const TaskSectionStyles = theme =>
  StyleSheet.create({
    card: {
      marginVertical: 6,
      marginHorizontal: 12,
      backgroundColor: theme.colors.surface,
    },
  });
